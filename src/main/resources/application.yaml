spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://localhost:5432/novikova'
    username: postgres
    password: 123
  jpa:
    properties:
      hibernate:
        dialect: 'org.hibernate.dialect.PostgreSQLDialect'
        format_sql: true
    hibernate:
      ddl-auto: none
  thymeleaf:
    cache: false
  mvc:
    hiddenmethod:
      filter:
        enabled: true







